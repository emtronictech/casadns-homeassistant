update_now:
  name: Update now
  description: Force CasaDNS to check the current public IP address and update the configured DNS records. When response data is requested, the current CasaDNS status is returned.
  fields: {}
